{
  "version": 2,
  "buildCommand": "npm run build",
  "routes": [
    { "src": "/api/(.*)", "dest": "dist/shared/infra/http/server.js" },
    { "src": "/(.*)", "dest": "dist/shared/infra/http/server.js" }
  ]
}
